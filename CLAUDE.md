# راهنمای کار روی این مخزن

⚠️ **این مخزن عمومی است.** هر چیزی که اینجا کامیت شود، برای همه‌ی دنیا
قابل دیدن است — و در تاریخچه‌ی گیت می‌ماند حتی اگر بعداً حذفش کنی.

## این مخزن چیست

ویترین محصول **دوام**: معرفی، مستندات نصب، و اسکرین‌شات. **صفر خط کد.**

سورس در مخزن خصوصی جداست: `m0000hamad/davam`.
درخت کاری روی ماشین توسعه: `E:\Ai-Folder\DAVAM\davam-showcase`

- `README.md` فارسی · `README.en.md` انگلیسی — هر دو باید هم‌زمان به‌روز شوند
- `index.html` صفحه‌ی GitHub Pages با تب واقعی فارسی/English
  → زنده روی `https://m0000hamad.github.io/davam-showcase/`
- `assets/screenshots/` تصاویر

## قواعدی که نباید شکسته شوند

**۱) نام هیچ شرکتی جز «دوام» نیاید.** نه مشتری، نه نام دکل‌ها
(`سپهر ۱/۲/۳` که در seed سامانه هست)، نه نام سامانه‌ی ERP قبلی. این
تصمیم صریح صاحب محصول است.

**۲) هیچ داده‌ی واقعی مشتری در تصاویر نباشد.** پیش از افزودن هر
اسکرین‌شات، خودت بازش کن و چک کن: نام یا عکس پرسنلی، نامه‌ی محرمانه،
شماره‌ی نامه، رقم مالی واقعی، یا نوار آدرس مرورگر که دامنه‌ی استقرار را
لو بدهد. یک نمونه‌ی ردشده در
`E:\Ai-Folder\DAVAM\_private-not-for-github\` نگه داشته شده.

**۳) توکن یا هر اعتبارنامه‌ای اینجا نوشته نشود** — حتی در مثال. همه‌ی
نمونه‌ها باید جای‌نگهدار باشند (`'توکنی-که-گرفته‌اید'`).

**۴) توضیحات باید توصیف قابلیت باشند، نه فهرست اسم ماژول‌ها.** فهرست‌کردن
منوی سامانه هم بی‌ارزش است هم ساختار داخلی محصول را لو می‌دهد.
به‌جای «درخواست کالا · سفارش خرید · رسید و بازرسی» بنویس «درخواست کالای
پیشرفته با رهگیری دقیق؛ در هر لحظه معلوم است روی میز چه کسی مانده».

## نکته‌های فنی صفحه

- Pages از شاخه‌ی `main` و ریشه سرو می‌شود؛ `.nojekyll` لازم است.
- تصاویر گالری در آرایه‌ی `SHOTS` داخل `index.html` تعریف می‌شوند. هر
  تصویری که لود نشود با `onerror` خودش را حذف می‌کند و اگر هیچ‌کدام لود
  نشوند کل بخش پنهان می‌ماند — پس صفحه هیچ‌وقت عکس شکسته نشان نمی‌دهد.
- در حالت راست‌چین مراقب `inset-inline` همراه `translateX` باش؛ یک‌بار
  درخشش پس‌زمینه‌ی قهرمان همین‌طور صفحه را افقی اسکرول‌پذیر کرد.
- `body` عمداً `overflow-x:clip` دارد نه `hidden` — چون `hidden` هدر
  sticky را می‌شکند.
- بعد از هر تغییر، عرض باریک را تست کن:
  `document.documentElement.scrollWidth > clientWidth` باید `false` باشد.
- شماره‌ی نسخه در سه جا تکرار شده (بج هر دو README و دو `pill` در
  `index.html`). موقع انتشار نسخه‌ی جدید همه را با هم عوض کن.
